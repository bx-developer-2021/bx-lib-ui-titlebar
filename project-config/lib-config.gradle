ext {

    GROUP = "co.bxvip.android.lib"
    localRepoPath = "file://${projectDir}/libs-pro-local-repo"
    isLocalReop = true

    libModelConfigs = [
            "lib-ui-widget"            : [
                    libName       : "lib-ui-titlebar",
                    libVersionName: "1.0.1",
                    libType       : "aar"
            ]
    ]
}